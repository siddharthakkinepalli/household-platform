# Gradle Properties for Household Platform

# Enable AndroidX
android.useAndroidX=true

# Suppress compileSdk warning
android.suppressUnsupportedCompileSdk=35

# Gradle parallel builds
org.gradle.parallel=true
org.gradle.workers.max=4

# Enable new resource processing
android.enableResourceOptimizations=true

# Kotlin
kotlin.code.style=official

# Fix jlink/Java compatibility issues
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
android.gradle.plugin.compatibility.ignoreCoreLibraryDesugaring=true
android.disableJdkImageTransform=true
